<div class="what-learn-section">
    <h2>What you will learn</h2>
    <ul>
        <% if (whatLearn && whatLearn.length > 0) { %>
            <% whatLearn.forEach(item => { %>
            <li>🗸 <%= item %></li>
            <% }) %>
        <% } else { %>
            <li>No learning outcomes specified.</li>
        <% } %>
    </ul>
</div>  